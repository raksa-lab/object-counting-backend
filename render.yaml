services:
  - type: web
    name: object-counting-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 240 --graceful-timeout 30 --keep-alive 5
